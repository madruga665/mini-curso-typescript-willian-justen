"use strict";
// Interfaces
// Implements
class CreateGameI {
    constructor(title, extra) {
        this.title = title;
        this.extra = extra;
    }
}
[1, 2, 3, "Xablau"];
const xablau = { a: "Xablau1", b: "Xablau2" };
// vantagem: quando estiver criando libs, prefira interfaces, porque sãpo mais extensíveis!
// criando objetos/classes (POO)
